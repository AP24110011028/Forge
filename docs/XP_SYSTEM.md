# XP system

XP is derived from planned effort, priority, and category. Completion writes one immutable ledger event with a unique `action:<id>:done` source key, so toggling cannot duplicate rewards. Skipped, frozen, missed, and deleted actions award nothing. Level requirements start at 500 XP and rise by approximately 16% per level.
